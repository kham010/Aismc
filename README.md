# مشروع AISMC 🌟
   منصة لإدارة المحتوى باستخدام الذكاء الاصطناعي.

   ## كيفية التشغيل 🔧
   1. تثبيت المتطلبات:
      ```bash
      cd backend && npm install
      cd ../frontend && npm install
